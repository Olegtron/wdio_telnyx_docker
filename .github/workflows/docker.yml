name: Tests in Docker container
on:
  push:
    branches: [master]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build image
      run: docker build . --file Dockerfile --tag olegtron
    - name: run tests in ocntainer
      run: docker run olegtron